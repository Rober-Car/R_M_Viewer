<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================= -->
    <!-- BASE THEME – RMViewer (MODO OSCURO REAL) -->
    <!-- ============================================= -->
    <style name="Base.Theme.RMViewer" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- FONDO GLOBAL -->
        <item name="android:windowBackground">@color/bg_dark</item>

        <!-- COLOR PRINCIPAL -->
        <item name="colorPrimary">@color/verde_neon_dark</item>

        <!-- TEXTO / ICONOS SOBRE PRIMARY -->
        <item name="colorOnPrimary">@color/black</item>

        <!-- SUPERFICIES (CardView, dialogs, etc.) -->
        <item name="colorSurface">@color/surface_dark</item>

        <!-- TEXTO SOBRE SUPERFICIES -->
        <item name="colorOnSurface">@color/text_primary_dark</item>

    </style>

    <style name="Theme.RMViewer" parent="Base.Theme.RMViewer"/>

</resources>
