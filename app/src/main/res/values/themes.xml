<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.RMViewer" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- FONDO GLOBAL DE TODA LA APP -->
        <item name="android:windowBackground">@color/verde_neon</item>

        <!-- COLOR DE LA TOOLBAR -->
        <item name="colorPrimary">@color/azul_rm</item>

        <!-- COLOR DEL TEXTO / ICONOS DE LA TOOLBAR -->
        <item name="colorOnPrimary">@color/white</item>

    </style>

    <style name="Theme.RMViewer" parent="Base.Theme.RMViewer" />
</resources>
